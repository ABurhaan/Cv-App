import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF1A1A2E);
  static const accent = Color(0xFF6B7CB4);
  static const secondary = Color(0xFF9B51E0);
  
  static const gradientStart = Color(0xFF2A3875);
  static const gradientEnd = Color(0xFF4A5B9B);
  
  static const cardBackground = Color(0xFF262640);
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFFB8B8D2);
  
  static final shadowColor = Colors.black.withOpacity(0.2);
  static final overlayColor = Colors.white.withOpacity(0.1);
}